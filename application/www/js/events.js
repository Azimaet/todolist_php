'use strict'

//With PHP, I don't need anymore all the JS function of my first Front Version. So I choice to keep only the toggle on the form
function onClickAddTask()
{
    // This function toggle the form (which is hidden by default)
    $("#todolist_form").slideToggle(200);
}
